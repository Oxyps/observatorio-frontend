import { createMuiTheme } from '@material-ui/core/styles';

const palette = {
	primary: { main: '#1cc41a', contrastText: '#FFFF' },
	secondary: { main: '#FF6D00', contrastText: '#FAFAFA' }
};

const themeName = 'Moss Green Blaze Orange Burro';

export default createMuiTheme({ palette, themeName });
